<section id="publications-timeline" class="publications-timeline-container">
  <% @publications.each do |publication| %>
  <%= render "publications/partials/timelinePartial", publication: publication %>
  <% end %>
</section>
