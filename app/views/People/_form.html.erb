<%= simple_form_for(person) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :firstName %>
    <%= f.input :lastName %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
